import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Anish Poudel - Economics Student & Research Assistant',
  description: 'Portfolio of Anish Poudel - Economics student at Michigan State University specializing in econometrics, data science, and entrepreneurship',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
